package click.erudosaba.mc.eminejobs2.skill

import org.bukkit.Material
import java.util.*

class SkillOption(val enabled : Boolean, val name : String, val needLevel : Int, val activeTime : Int, val interval : Int, val description : Array<String>, val icon : Material) {
}